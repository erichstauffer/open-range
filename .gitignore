node_modules
.next
out
build
.DS_Store
*.pem
.env*
!.env.example
.vercel
next-env.d.ts
*.tsbuildinfo

# Preview output. public/og.png is the only committed binary, and only because
# crawlers cannot run the generator; audio previews have no such excuse.
*.wav
*-preview.png
